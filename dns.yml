---
- hosts: dns
  roles: [hajidi131.jms_infra.common, hajidi131.jms_infra.mydns]
  # =====================================================================
# 파일 위치 : ~/ansible_infra/dns.yml
# 역할      : DNS 서버만 단독으로 구축하는 개별 플레이북.
#             site.yml(전체) 대신 DNS 만 다시 배포하거나 테스트할 때 사용.
# ==============================================================