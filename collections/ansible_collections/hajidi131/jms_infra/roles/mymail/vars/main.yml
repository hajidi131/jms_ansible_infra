# =====================================================================
# 파일 위치 : roles/mymail/vars/main.yml
# 역할      : 메일 사용자 비밀번호 변수 (강한 우선순위).
#             FTP 와 동일한 패턴으로 vault 변수가 없으면 기본값 사용.
# =====================================================================
---
mail_password: "{{ vault_mail_password | default('jms1.') }}"