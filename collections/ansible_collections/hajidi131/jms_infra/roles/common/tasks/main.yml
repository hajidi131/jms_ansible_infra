---
- name: Install base packages - {{ base_pkgs }}
  ansible.builtin.dnf:
    name: "{{ base_pkgs }}"
    state: present

- name: Deploy /etc/hosts from template
  ansible.builtin.template:
    src: hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: '0644'